const createFormDataFromProjectData = (projects) => {
  const formData = new FormData();
  for (const [key, value] of Object.entries(formData)) {
  }
  // Append all text fields
  //   formData.append("title", projectData.title);
  //   formData.append("challenge", projectData.challenge);
  //   formData.append("solution", projectData.solution);
  //   formData.append("donationUsage", projectData.donationUsage);
  //   formData.append("futureImpact", projectData.futureImpact);
  //   formData.append("listOfTags", JSON.stringify(projectData.listOfTags));
  //   formData.append("links", JSON.stringify(projectData.links));
  //   formData.append("targetAmount", projectData.targetAmount.toString());
};
